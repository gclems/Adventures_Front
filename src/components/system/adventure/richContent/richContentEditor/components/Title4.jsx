import BaseTag from "./BaseTag";

function Title4({ textareaRef }) {
  return (
    <BaseTag textareaRef={textareaRef} tag="####" title="Titre niveau 4">
      H4
    </BaseTag>
  );
}

export default Title4;
