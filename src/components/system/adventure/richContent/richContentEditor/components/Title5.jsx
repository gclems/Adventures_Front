import BaseTag from "./BaseTag";

function Title5({ textareaRef }) {
  return (
    <BaseTag textareaRef={textareaRef} tag="#####" title="Titre niveau 5">
      H5
    </BaseTag>
  );
}

export default Title5;
